//! Quality manifest resolution and handler support for attractor pipelines.
//!
//! This crate provides the `resolution` module for locating and parsing `pas.toml`
//! manifests, used by both the preflight checker and the quality handler at runtime.

pub mod resolution;
